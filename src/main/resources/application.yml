spring:
  datasource:
    url: jdbc:mysql://localhost:3306/backend_test?serverTimezone=Asia/Seoul
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        # show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect


# 파일 경로 설정
file:
  dir: C:/Users/X/files/